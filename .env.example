# Variables de entorno para Docker Compose
# Copia este archivo a .env.local si quieres valores locales diferentes

# --- Supabase ---
SUPABASE_URL=https://[PROJECT_REF].supabase.co
SUPABASE_SERVICE_KEY=[YOUR_SERVICE_KEY]
SUPABASE_JWT_SECRET=[YOUR_JWT_SECRET]

# --- Frontend / Vite ---
VITE_SUPABASE_URL=https://[PROJECT_REF].supabase.co
VITE_SUPABASE_ANON_KEY=[YOUR_ANON_KEY]

# --- Google Maps API Key ---
# Obtén tu clave en: https://console.cloud.google.com/
# Reemplaza con tu API key real
VITE_GOOGLE_MAPS_API_KEY=[YOUR_GOOGLE_MAPS_API_KEY]

# --- Stripe ---
STRIPE_SECRET_KEY=sk_test_[YOUR_STRIPE_SECRET]
STRIPE_PUBLISHABLE_KEY=pk_test_[YOUR_STRIPE_PUBLISHABLE]

# --- Database Configuration (Supabase Connection Pooling) ---
# Connection Pooling - Modo Producción (recomendado)
# Obtén estos datos en: https://supabase.com/dashboard/project/[PROJECT_ID]/settings/database
# Selecciona "Connection pooling" en el selector de tipo de conexión
DATABASE_URL=postgresql://postgres.[PROJECT_REF]:[YOUR_PASSWORD]@aws-[REGION].pooler.supabase.com:6543/postgres?sslmode=require

# Variables individuales (alternativa si GORM no lee DATABASE_URL)
DB_HOST=aws-[REGION].pooler.supabase.com
DB_PORT=6543
DB_USER=postgres.[PROJECT_REF]
DB_PASSWORD=[YOUR_DATABASE_PASSWORD]
DB_NAME=postgres
DB_SSLMODE=require

# Configuración de pool de conexiones
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=300
